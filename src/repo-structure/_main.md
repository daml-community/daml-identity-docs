# Repository Specification

There is a prescribed file layout for keys in the repository so that it can be recognised by the **DAML package manager**. Additionally the repository is protected by a `gittuf` security policy. Both are documented in this specification.
